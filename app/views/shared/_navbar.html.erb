<nav>
	<ul>

		<% if !user_signed_in? %>
		  <a href="<%= new_user_session_path %>">Sign In</a>
		<% else %>
		  <a href=""><%= link_to "Sign Out", destroy_user_session_path, data: {turbo_method: "delete"} %> </a>
	      <a href="<%= new_post_path %>">New Post</a>
		<% end %>

		<a href=""></a>
		<a href=""></a>
		<a href=""></a>
	</ul>
</nav>